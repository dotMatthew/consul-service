spring:
  cloud:
    consul:
      host: consul-client.consul.svc.cluster.local
      port: 8500
      discovery:
        hostname: "test"
        instance-id: "test-instance-id"
        fail-fast: true
      config:
        enabled: false